- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc:
   ~/.gitconfig:

- create:
    - ~/Workspace

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
  -
    command: ./setup_vscode.zsh
    stdout: true
    stderr: true

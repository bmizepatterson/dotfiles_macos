- defaults:
    link:
      relink: true

- clean: ['~']

- link:
   ~/.zshrc:
   ~/.gitconfig:

- create:
    - ~/Workspace

- shell:
  -
    command: ./setup_homebrew.zsh
    stdout: true
    stderr: true
